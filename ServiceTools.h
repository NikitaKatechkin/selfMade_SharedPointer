#ifndef SERVICETOOLS_H_INCLUDED
#define SERVICETOOLS_H_INCLUDED

#include <iostream>
#include <ctime>
#include <cstdlib>

namespace ServiceTools
{
    void RandNumArrayFill(int *arr, int arr_size);
    void PrintArray(int *arr, int arr_size);
    void SwapPair(int *first, int *second);
}

#endif // SERVICETOOLS_H_INCLUDED
